#include <stdio.h>
#include <stdlib.h>
#include "lib.h"

int main()
{
    // RB_tests();
    // small_RB_tests();

    // Node *external = create_external();
    // Node *root = external;
    // int vector[] = {10, 5, 15, 2, 7, 12};
    // // int vector[] = {
    // //     5,
    // //     2,
    // //     8,
    // //     1,
    // //     4, 7, 9, 3, 6};

    // int n = 6;

    // for (int j = 0; j < n; j++)
    // {
    //     insert_RB(vector[j], &root, external);
    // }

    // // remove_node(2, &root, external);
    // pre_order(root, external);

    // printf("Is Red black Tree: %s\n", is_RB_tree(root, external) ? "True" : "False");

    return 0;
}
